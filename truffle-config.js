'use strict';

// Kept as an explicit guard for old commands and deployment bookmarks. The
// discontinued Truffle/Ganache toolchain is no longer installed or supported.
throw new Error('Truffle is retired. Run npm run build / npm test for local verification, or use the Hardhat deployment workflow.');
