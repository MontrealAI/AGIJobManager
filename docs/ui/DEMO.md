# Demo Mode

Set `NEXT_PUBLIC_DEMO_MODE=1` and use `?scenario=default` or `?scenario=ops-incident`.

| Scenario | Purpose |
| --- | --- |
| default | full lifecycle + malformed URI + huge values |
| ops-incident | paused + settlement pause + degraded RPC |
