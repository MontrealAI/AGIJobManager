# Demo Mode

Run with `NEXT_PUBLIC_DEMO_MODE=1` to use deterministic fixtures.

| Scenario | Intent |
|---|---|
| baseline | Full lifecycle coverage + malformed URI edge cases |
| degraded-paused | Shows paused + settlementPaused + degraded RPC banners |
