# Demo Mode

Run with `NEXT_PUBLIC_DEMO_MODE=1` to use deterministic fixtures.

- Read-only pages are fully navigable.
- Write actions are shown but disabled.
- Includes malformed URI and huge value edge-cases.

Generate demo visuals:

```bash
npm run docs:screenshots
```
