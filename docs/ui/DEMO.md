# Demo Mode

Run with `NEXT_PUBLIC_DEMO_MODE=1`.

Scenarios include open, assigned, completion-requested, disputed, settled, expired, malformed URIs, huge values.

Generate visual assets:

```bash
npm run docs:screenshots
```
