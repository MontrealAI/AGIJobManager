# UI Overview

Institutional-grade Next.js dApp + ops console with read-only-first behavior and demo-mode deterministic fixtures.

- Routes: `/`, `/jobs`, `/jobs/[jobId]`, `/admin`, `/design`, `/demo`
- Safety: simulation-first write framework, link allowlist, strict headers.
- Visuals: Mermaid diagrams + generated SVG plates.
