# UI Overview

Institutional-grade dapp + ops console for AGIJobManager with read-only-by-default workflows, optional wallet actions, and deterministic Demo Mode.

- Read-only pages: dashboard, jobs list, job detail, config/admin view.
- Wallet mode: simulation-first writes only.
- Demo mode: deterministic fixtures for CI and docs.
