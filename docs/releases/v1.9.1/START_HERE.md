# Start here — AGIJobManager v1.9.1

1. Read `RELEASE_NOTES.md` and `VALIDATION.md` for changes, evidence and limits.
2. Open `source/README.md` for the updated system overview and `source/docs/START_HERE.md` for participant instructions.
3. Open `agijobmanager-usdc.html` for the manual USDC console. Configure only your verified instance; the software release supplies no live manager.
4. Use `source/docs/OPERATIONS/QUALIFIED_ADMISSION.md` for current schema 6/7 operator-budget policy, all five CLI inputs and the complete synthetic example. No new review retainers are supported by the operating boundary.
5. Use `source/docs/OPERATIONS/GENERAL_COMPUTER_WORK.md` and `COMPUTER_USE_CURRENT.md` for actual runtime coverage and current integration research.
6. Deploy and recover using `source/hardhat/README.md`, `source/docs/RELEASE_GUIDE.md` and the complete `source/docs/REFERENCE/COMMANDS.md` inventory. Existing review-escrow commands only recover source verification; they cannot deploy a new escrow.
7. Verify downloads against `SHA256SUMS.txt` and `RELEASE_MANIFEST.json`.

Separate private downloads: Fleet 1.7.1, Agent 1.13.1, Node 2.14.1. Their offline START_HERE.html pages and regenerated READMEs provide setup, upgrade, commands and verification. Preserve original operator data, journals, earned claims and old recovery policies. Actual Mac/provider commissioning is still required before production intake.
